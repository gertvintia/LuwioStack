// Domain — parse & validate with `Iban.parse(...)`. React-free (uses @luwio/country for `country()`).
// React bindings live at `@luwio/iban/react`.
export { Iban } from './domain/iban'
export type { IIban } from './types'
