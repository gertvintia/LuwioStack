// Domain — parse & validate with `Phone.parse(...)`. React-free (uses google-libphonenumber and
// @luwio/country). React bindings live at `@luwio/phone/react`.
export { Phone } from './domain/phone'
export { type IPhone, type PhoneFormat, PhoneNumberType } from './types'
